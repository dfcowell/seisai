import styled from "styled-components";
import { height } from "./Menu";

export const PageContainer = styled.div`
  box-sizing: border-box;
  display: flex;
  height: 100%;
  padding-top: ${height};
`;
